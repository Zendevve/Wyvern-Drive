---
phase: 04-design-system-sidebar-navigation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - web/index.html
  - web/src/styles/tokens.css
  - web/src/styles/global.css
  - web/src/styles/components.css
  - web/src/components/AppShell.tsx
  - web/src/components/Sidebar/Sidebar.tsx
  - web/src/components/Sidebar/StorageGauge.tsx
  - web/src/components/Sidebar/CategoryBreakdown.tsx
autonomous: false
requirements:
  - THEME-01
  - THEME-02
  - THEME-03
  - SIDEBAR-01
  - SIDEBAR-02
  - SIDEBAR-03
must_haves:
  truths:
    - "Application layout uses a wide 260px left sidebar navigation container."
    - "Branding header shows the Artano wyvern/claw logo mark with proper labels."
    - "Sidebar displays a semi-circular storage progress gauge showing used/total bytes."
    - "Sidebar lists file category breakdown with custom line indicators."
    - "Theme colors use black background and white borders/accents."
  artifacts:
    - path: "web/src/components/Sidebar/Sidebar.tsx"
      provides: "Sidebar navigation shell with branding and layout"
    - path: "web/src/components/Sidebar/StorageGauge.tsx"
      provides: "SVG semi-circular storage progress arc gauge"
    - path: "web/src/components/Sidebar/CategoryBreakdown.tsx"
      provides: "File category breakdown list widget"
    - path: "web/src/styles/tokens.css"
      provides: "Artano branding theme color tokens"
  key_links:
    - from: "web/src/components/AppShell.tsx"
      to: "web/src/components/Sidebar/Sidebar.tsx"
      via: "React element import and rendering"
      pattern: "import.*Sidebar"
---

<objective>
Implement the core design system styling and navigation sidebar according to the Artano brand guidelines.

Purpose: Build a premium, high-contrast monochrome design and standard desktop-grade cloud storage navigation shell.
Output: Integrated Outfit typography, monochrome colors, repeating dot-matrix halftone patterns, left-hand sidebar with SVG semi-circular storage gauge, and file category stats list.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
@~/.gemini/antigravity/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@web/index.html
@web/src/styles/tokens.css
@web/src/styles/global.css
@web/src/styles/components.css
@web/src/components/AppShell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update design tokens and global styles</name>
  <files>web/index.html, web/src/styles/tokens.css, web/src/styles/global.css, web/src/styles/components.css</files>
  <read_first>web/index.html, web/src/styles/tokens.css, web/src/styles/global.css</read_first>
  <action>
    - Update `web/index.html` to swap 'Inter' Google Font with 'Outfit:wght@400;500;600;700'.
    - Update `tokens.css` with the Artano monochrome theme:
      - `--bg-base`: `#0D0E12`
      - `--bg-surface-1`: `#16171F`
      - `--bg-surface-2`: `#21232D`
      - `--bg-glass`: `rgba(22, 23, 31, 0.75)`
      - `--border-subtle`: `#262933`
      - `--border-strong`: `#383D4D`
      - `--text-primary`: `#FFFFFF`
      - `--text-secondary`: `#A0A5B5`
      - `--text-muted`: `#626775`
      - `--accent`: `#FFFFFF`
      - `--accent-hover`: `#E6E6E6`
      - `--accent-soft`: `rgba(255, 255, 255, 0.08)`
    - Update `global.css` to use `'Outfit', sans-serif` as body font.
    - In `global.css`, declare a repeating white dot pattern utility:
      ```css
      .dot-pattern {
        background-image: radial-gradient(rgba(255, 255, 255, 0.15) 1px, transparent 1px);
        background-size: 16px 16px;
      }
      ```
    - Update headings in `components.css` to use tight letter-spacing (`letter-spacing: -0.03em`).
  </action>
  <verify>npm run build in web/ directory passes</verify>
  <acceptance_criteria>
    - `tokens.css` contains `#0D0E12` and `#16171F`.
    - `global.css` contains `font-family: 'Outfit'` and `.dot-pattern` rule.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Build sidebar navigation and gauge components</name>
  <files>web/src/components/Sidebar/StorageGauge.tsx, web/src/components/Sidebar/CategoryBreakdown.tsx, web/src/components/Sidebar/Sidebar.tsx</files>
  <read_first>web/src/components/icons.tsx</read_first>
  <action>
    - Create `web/src/components/Sidebar/StorageGauge.tsx`:
      - Render a semi-circular progress arc using SVG `<path>` or `<circle>` elements.
      - Center text showing percentage and bytes used (e.g. `24.5 GB of 100 GB`).
      - Apply a clean glowing white drop-shadow filter to the filled arc path.
    - Create `web/src/components/Sidebar/CategoryBreakdown.tsx`:
      - Render rows for Documents, Images, Videos, Audio, and Others with icons imported from `icons.tsx`.
      - Show thin horizontal progress lines for proportional file sizes in white.
    - Create `web/src/components/Sidebar/Sidebar.tsx`:
      - Render header with sharp 3-prong claw SVG icon, "Artano" title, and "Wyvern Drive" subtitle.
      - Render NavLink items for "My Drive" (active path: `/drive`), "Recent" (disabled), and "Trash" (disabled).
      - Render the `<StorageGauge />` and `<CategoryBreakdown />` widgets underneath.
      - Style active sidebar item with a left vertical white indicator line and a dot prefix.
  </action>
  <verify>npm run build in web/ directory passes</verify>
  <acceptance_criteria>
    - `Sidebar.tsx` renders the claw logo mark and navigation links.
    - `StorageGauge.tsx` renders SVG arc path.
    - `CategoryBreakdown.tsx` renders category line list.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Integrate Sidebar and adjust layout sizing</name>
  <files>web/src/components/AppShell.tsx, web/src/styles/components.css</files>
  <read_first>web/src/components/AppShell.tsx</read_first>
  <action>
    - In `AppShell.tsx`, replace the `<aside className="app-rail">` block with the new `<Sidebar />` component.
    - In `components.css`, update `.app-shell` grid-template-columns to `260px 1fr` and update grid-template-areas from `rail` to `sidebar` or align styles.
    - Apply dot pattern backgrounds to container elements to make the dark theme feel premium.
  </action>
  <verify>npm run build in web/ directory passes</verify>
  <acceptance_criteria>
    - `AppShell.tsx` renders `<Sidebar />`.
    - `.app-shell` in `components.css` sets columns to `260px 1fr`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Start local development server</name>
  <action>Run `npm run dev` in the `web` workspace directory in the background.</action>
  <verify>Verification of server startup output</verify>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Artano Brand Sidebar Layout & Storage Gauge</what-built>
  <how-to-verify>Visit the local web app dev URL (usually http://localhost:5173). Verify that the theme is premium monochrome dark, the sidebar is 260px wide, Outfit typography is active, and the semi-circular storage gauge displays clean SVG arcs.</how-to-verify>
  <resume-signal>Type "approved" to continue</resume-signal>
</task>

</tasks>

<verification>
- [ ] npm run build in web/ succeeds without TypeScript or compilation errors.
- [ ] Visual verification of the dashboard sidebar and storage gauge is approved.
</verification>

<success_criteria>
- All tasks completed successfully.
- Web app builds without errors.
- Layout integrates the new 260px wide sidebar navigation with custom storage widgets.
</success_criteria>

<output>
After completion, create `.planning/phases/04-design-system-sidebar-navigation/04-01-SUMMARY.md`
</output>
