---
wave: 0
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vitest.config.ts
  - tests/setup.ts
  - src/index.ts
  - src/app.ts
autonomous: true
---

# Plan 01-01: Project Scaffolding & Setup

Initialize the Wyvern Drive backend project structure, dependencies, TypeScript config, Vitest setup, and main Fastify application entry point.

## Traceability

- **Decisions Covered:** D-01 (Fastify selection)

## must_haves

- [ ] Fastify application initialized and starts successfully
- [ ] TypeScript configuration file `tsconfig.json` set up for modern Node.js
- [ ] Vitest setup file and config file present and executing tests
- [ ] Test command `npm run test` exits 0 with a basic test suite passing

## Tasks

<tasks>
<task id="1-01-01" type="auto" tdd="false">
  <name>Initialize Package.json and Dependencies</name>
  <description>Create package.json and install typescript, ts-node, fastify, @fastify/multipart, @discordjs/rest, discord-api-types, jsonwebtoken, and better-sqlite3 (for future use), along with development dependencies like vitest and typescript types.</description>
  <read_first>
    <file>package.json</file>
  </read_first>
  <action>
    Create a new package.json with dependencies:
    - fastify
    - @fastify/multipart
    - @discordjs/rest
    - discord-api-types
    - jsonwebtoken
    - dotenv
    - better-sqlite3
    DevDependencies:
    - vitest
    - typescript
    - ts-node
    - @types/node
    - @types/jsonwebtoken
    - @types/better-sqlite3
    Define scripts:
    - "dev": "ts-node src/index.ts"
    - "test": "vitest run"
    Install all dependencies using npm install.
  </action>
  <acceptance_criteria>
    - package.json exists and contains correct dependency entries
    - node_modules folder exists and is populated
    - npm run test command is registered in package.json
  </acceptance_criteria>
</task>

<task id="1-01-02" type="auto" tdd="false">
  <name>Configure TypeScript and Vitest</name>
  <description>Initialize tsconfig.json and vitest.config.ts for the project.</description>
  <read_first>
    <file>package.json</file>
  </read_first>
  <action>
    Create tsconfig.json with:
    - compilerOptions: module: "commonjs" or "NodeNext", target: "ES2022", moduleResolution: "node", esModuleInterop: true, strict: true, outDir: "./dist"
    Create vitest.config.ts with standard configuration pointing tests to a tests/ directory.
  </action>
  <acceptance_criteria>
    - tsconfig.json exists
    - vitest.config.ts exists and compiles without error
  </acceptance_criteria>
</task>

<task id="1-01-03" type="auto" tdd="false">
  <name>Scaffold Fastify Application and Status Endpoint</name>
  <description>Create src/app.ts containing the Fastify app setup and src/index.ts to start the listener.</description>
  <read_first>
    <file>package.json</file>
  </read_first>
  <action>
    Create src/app.ts:
    - Import fastify
    - Build application instance
    - Register a status endpoint GET /status returning { status: "OK", timestamp: string }
    - Export a buildApp function
    Create src/index.ts:
    - Import buildApp from ./app
    - Listen on port 3000 (or PORT env var)
  </action>
  <acceptance_criteria>
    - src/app.ts exists and exports buildApp
    - src/index.ts exists and starts server successfully
    - GET /status endpoint returns 200 with JSON { status: "OK" }
  </acceptance_criteria>
</task>

<task id="1-01-04" type="auto" tdd="true">
  <name>Setup Test Suite and Base Status Test</name>
  <description>Create tests/setup.ts and tests/app.test.ts to verify the status endpoint using app.inject().</description>
  <read_first>
    <file>src/app.ts</file>
  </read_first>
  <action>
    Create tests/setup.ts for any shared test configuration.
    Create tests/app.test.ts:
    - Import buildApp from ../src/app
    - Inject GET /status route using app.inject()
    - Assert status code is 200 and response body contains status: "OK"
    Run "npm run test" to verify.
  </action>
  <acceptance_criteria>
    - tests/app.test.ts runs and passes
    - npm run test exits successfully (code 0)
  </acceptance_criteria>
</task>
</tasks>
