---
phase: 10-agents-md
plan: 01
type: execute
wave: 5
depends_on:
  - 09-01
files_modified:
  - AGENTS.md
autonomous: true
requirements:
  - UI-01
  - UI-02
user_setup: []

must_haves:
  truths:
    - "AGENTS.md exists at the repo root and contains the design discipline rule set: dials, icon library, shape scale, z-index scale, semantic colors, theme tokens, motion rules, a11y rules, performance rules, anti-patterns, and explicit out-of-scope rules"
    - "A future agent reading AGENTS.md can answer: 'what icon library do I use?' and 'what border-radius does a card get?' without grepping"
    - "AGENTS.md explicitly bans marketing-page rules (hero, bento, marquee, eyebrow caps, zigzag cap, trusted-by wall, scroll cue, premium consumer palette, Marrow-style spec sheet) and explains why they don't apply to a dashboard/product UI"
  artifacts:
    - path: "AGENTS.md"
      provides: "Durable design + a11y + perf + token rule set"
      contains: "# AGENTS.md"
  key_links:
    - from: "AGENTS.md"
    - to: "src/constants/tokens.ts"
      via: "referenced as the source of truth for SHAPE_SCALE / Z_INDEX / SEMANTIC_COLORS"
      pattern: "src/constants/tokens.ts"
    - from: "AGENTS.md"
    - to: "src/index.css"
      via: "referenced as the source of truth for theme tokens"
      pattern: "src/index.css"
---

<objective>
Write AGENTS.md at the repo root as the durable rule set that future agents and humans must follow when working in this codebase. Consolidate the dial values, the icon library, the shape / z-index / semantic-color scales, the motion rules, the a11y rules, the perf rules, and the explicit out-of-scope marketing-page rules.

Purpose: The previous four phases established conventions by example and by token, but conventions drift without a written record. AGENTS.md is the durable artifact that codifies what the design discipline is, what is in scope, and what is explicitly out of scope (marketing-page patterns are out of scope for a dashboard).

Output: A single AGENTS.md at the repo root that is < 400 lines, scannable, and references the live source files (tokens.ts, index.css, icon-map.ts, useReducedMotion.ts) rather than duplicating their content.

Scope guard: AGENTS.md documents ONLY what was actually established in Phases 06-09. It does NOT add new rules, does NOT introduce marketing-page patterns, does NOT rewrite the README, and does NOT alter package.json. It does NOT include a "premium consumer palette" check (banned), does NOT include hero / bento / marquee / eyebrow-cap / zigzag-cap / trusted-by-wall / scroll-cue rules (all out of scope for this dashboard).
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/index.css
@src/constants/tokens.ts
@src/components/icon-map.ts
@src/hooks/useReducedMotion.ts
@src/App.tsx
@src/components/SettingsPanel.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write AGENTS.md at the repo root with the design discipline rule set</name>
  <files>AGENTS.md</files>
  <action>
Create AGENTS.md at the repo root. The file must be < 400 lines, scannable, and reference live source files (not duplicate their content). Use this exact section structure:

```markdown
# AGENTS.md

> Durable rule set for the Wyvern Drive codebase. Every agent and human must read this before editing UI, theme, icons, motion, or accessibility. Source of truth files are linked, not copied.

## 1. Project Snapshot

- **Product:** Wyvern Drive — self-hosted encrypted PWA file-storage.
- **Stack:** Vite 6 + React 19 + TypeScript 5.7 + Tailwind v4 (via @tailwindcss/vite) + Zustand 5 + Radix UI + idb + vitest 4 + Playwright 1.60.
- **Path:** repo root.
- **Aesthetic:** Signal Orange (#FF5A00) + Clash Display (display) + Satoshi (body) + brutalist-glass surfaces. Dashboard / product UI class — NOT a marketing site.

## 2. Design Dials

| Dial | Value | Notes |
|------|-------|-------|
| DESIGN_VARIANCE | 4 | One shape scale, one z-index scale, one color palette. |
| MOTION_INTENSITY | 3 | Subtle state transitions; infinite animations gated. |
| VISUAL_DENSITY | 6 | High info density; cards/tables; minimal whitespace bloat. |

## 3. Tokens (Authoritative Source)

- **Theme tokens:** `src/index.css` (`@theme` block + `:root` + `.dark`).
  - Foreground: `--color-foreground`, `--color-text-muted`, `--color-border`
  - Surfaces: `--color-background`, `--color-card`, `--color-card-hover`
  - Brand: `--color-primary` (Signal Orange), `--color-primary-hover`
  - Semantic: `--color-success`, `--color-warning`, `--color-error`, `--color-destructive`
  - Soft tints: `--color-surface-success`, `--color-surface-warning`, `--color-surface-error`
- **Shape scale:** `src/constants/tokens.ts` → `SHAPE_SCALE`.
  - `SHAPE_SCALE.input` → `rounded-lg` (form controls, small buttons)
  - `SHAPE_SCALE.card` → `rounded-xl` (cards, modals, panels)
  - `SHAPE_SCALE.pill` → `rounded-full` (chips, status dots, round buttons)
- **Z-index scale:** `src/constants/tokens.ts` → `Z_INDEX`.
  - `base` (z-0) → `raised` (z-10) → `dropdown` (z-20) → `sticky` (z-30) → `overlay` (z-40) → `modal` (z-50) → `toast` (z-[60]) → `skipLink` (z-[100]).
- **Semantic colors:** `src/constants/tokens.ts` → `SEMANTIC_COLORS` (use `{bg, text, soft}` keys).

## 4. Icons

- **Library:** `@phosphor-icons/react` at `^2.1.7` (MIT, Phosphor Icons team).
- **Weight:** always `weight="regular"` (= strokeWidth 1.5).
- **Sizing:** 14px for inline labels, 18px for sidebar nav, 20px for action buttons, 28–48px for hero-of-control (audio album, drop overlay).
- **File-type icon resolver:** `src/components/icon-map.ts` → `getFileIcon(mimeType)`. Use this for every file-type glyph — never inline an emoji.
- **A11y:** every icon-only button must have an `aria-label` on the parent button. Icons themselves should carry `aria-hidden="true"`.

## 5. Motion

- **Source of truth:** `src/hooks/useReducedMotion.ts` → `useReducedMotion(): boolean`.
- **CSS gate:** `src/index.css` contains a `@media (prefers-reduced-motion: reduce)` block that collapses `animate-pulse`, `animate-bounce`, `animate-spin`, `animate-in`, `animate-[spin_*s_linear_infinite]`. Do not delete this block.
- **Rule:** ALL infinite / looping animations MUST be gated behind `useReducedMotion()`. Hover / state-change transitions (transition-colors, transition-[transform,opacity]) are exempt.
- **Banned:** neon outer glow (`shadow-[0_0_*_rgba(...)]`), parallax, scroll-cue animations, staggered list reveals.
- **Allowed:** `transition-colors`, `transition-[transform,opacity]`, `transition-[transform,colors]`, `transition-all` (only when the change set actually spans 3+ properties — the audio player container and sidebar width collapse are the two legitimate cases).
- **Viewport:** always `h-[100dvh]`, never `h-screen`.

## 6. Accessibility

- WCAG AA contrast in BOTH light and dark themes.
- Every icon-only button has an `aria-label`.
- Skeleton / loading text uses `aria-live="polite"` where appropriate (Toast container already does).
- Keyboard: every interactive element is reachable via Tab; focus-visible outline uses `--color-primary` (already declared globally in `src/index.css`).
- No `useState` for scroll position. No `window.addEventListener('scroll', ...)` for visual effects.
- Skip-to-content link is rendered in `App.tsx` with `Z_INDEX.skipLink`.

## 7. Performance

- **Fonts:** self-hosted in `public/fonts/*.woff2`. Preloaded in `index.html` with `crossorigin`. Do NOT re-introduce `@import` from any third-party font CDN.
- **Images:** every `<img>` MUST have explicit `width`, `height`, `decoding="async"`, and `loading="lazy"` (unless above-the-fold hero, which this app has none of).
- **Viewport:** `h-[100dvh]`, not `h-screen`.

## 8. Anti-Patterns (Banned)

- Emoji as UI icon (anywhere in JSX). Use Phosphor via `getFileIcon` or direct import.
- Hardcoded Tailwind color literals (`text-emerald-500`, `bg-blue-500/10`, etc.). Use the tokens.
- Raw `z-N` or `z-[N]` classes. Use `Z_INDEX.*`.
- `rounded-2xl`, `rounded-3xl`. Use the SHAPE_SCALE.
- `shadow-[0_0_*]` (outer glow / neon). Use ring + saturated bg instead.
- `transition-all` on elements where only 1–2 properties change.
- `h-screen`. Use `h-[100dvh]`.
- The em-dash character `—` (U+2014) in user-facing strings. ASCII en-dash `-` or words like "to"/"and" are fine.
- New third-party dependencies without a written justification in the SUMMARY.

## 9. Out of Scope (Explicitly)

This is a dashboard / product UI. The following marketing-page patterns are NOT in scope and must NEVER be added:

- Hero sections, hero CTAs, hero animations
- Bento grids
- Marquees / scrollers / tickers
- Eyebrow caps (small uppercase text above section titles)
- Zigzag / alternating layout caps
- Trusted-by walls / logo strips / "as seen in"
- Scroll-cue chevrons / "scroll to explore"
- Premium consumer palette checks (Inter + neutral grays)
- Marrow-style spec sheet rewrites
- Holographic / iridescent / glassmorphic-on-glass backgrounds
- Carousel sections, "infinite" loops of content

If a request resembles any of the above, decline with a one-sentence reason and propose a dashboard-appropriate alternative.

## 10. Working with this repo

- Tests: `npm run test` (vitest unit + integration), `npm run test:e2e` (Playwright).
- Build: `npm run build` (tsc + vite build).
- Dev: `npm run dev`.
- Phase plans: `.planning/phases/{NN-name}/NN-NN-PLAN.md` → execute via `/gsd-execute-phase {NN}`.
- After every phase, run `npm run test && npm run build` to verify no regression.

## 11. When in Doubt

- Read the source files linked above before guessing.
- If a rule seems to conflict with a new request, the tokens file wins. Refactor the request, not the tokens.
- If you find yourself wanting to introduce a new dependency, write the justification in the phase SUMMARY first and stop.
```

Write this file at the repo root. Do NOT add marketing-page content, do NOT add a "premium consumer palette" check section, do NOT add a hero/bento/marquee example. The file is documentation of the rules, not an introduction of new ones.

After writing, run `npm run test` and `npm run build` to confirm the file does not break the build (AGENTS.md is not imported by the app, so this is a sanity check that you didn't accidentally add a syntax error somewhere else).
  </action>
  <verify>
<automated>test -f AGENTS.md && wc -l AGENTS.md && echo "---SECTIONS---" && grep -E "^## " AGENTS.md && echo "---MARKETING-CHECK---" && (rg -n "hero|bento|marquee|eyebrow|zigzag|trusted-by|scroll-cue|premium consumer palette|Marrow" AGENTS.md | grep -v "Out of Scope" | grep -v "If a request"; if [ $? -eq 0 ]; then echo "FAIL: marketing rule snuck into active sections"; exit 1; else echo "PASS: marketing rules only appear in the Out of Scope section"; fi) && echo "---BUILD---" && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
- AGENTS.md exists at the repo root
- AGENTS.md is under 400 lines
- AGENTS.md contains the 11 sections (Project Snapshot, Design Dials, Tokens, Icons, Motion, Accessibility, Performance, Anti-Patterns, Out of Scope, Working with this repo, When in Doubt)
- Marketing-page rules (hero, bento, marquee, eyebrow caps, zigzag, trusted-by wall, scroll cue, premium consumer palette, Marrow) appear ONLY in section 9 (Out of Scope) — never in active guidance
- `npm run build` exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AGENTS.md → future agents/humans | Documentation is read on every edit, not enforced by tooling |
| Phase SUMMARY → audit trail | The SUMMARY is the change record; AGENTS.md is the rule |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Repudiation | Rules drift over time | mitigate | AGENTS.md references the LIVE token files (`src/constants/tokens.ts`, `src/index.css`, `src/components/icon-map.ts`, `src/hooks/useReducedMotion.ts`) so the doc and the code can't drift silently |
| T-10-02 | Tampering | Marketing rules leak into active sections | mitigate | Grep gate verifies marketing vocabulary appears only in the "Out of Scope" section |
| T-10-SC | Tampering | npm installs | accept | No new dependencies in this phase |
</threat_model>

<verification>
1. `test -f AGENTS.md` succeeds
2. `wc -l AGENTS.md` shows < 400 lines
3. `grep -E "^## " AGENTS.md` lists the 11 sections
4. `rg -n "hero|bento|marquee|eyebrow|zigzag|trusted-by|scroll-cue|premium consumer palette|Marrow" AGENTS.md` returns matches ONLY in the "Out of Scope" section
5. `npm run test` exits 0 (AGENTS.md is a doc — no functional impact; sanity check that nothing else broke)
6. `npm run build` exits 0
</verification>

<success_criteria>
- AGENTS.md exists, is under 400 lines, and has the 11 required sections
- All cross-references point to live source files
- Marketing-page rules appear ONLY in the Out of Scope section
- All 28 tests still pass; build succeeds
- No functional code changes (this is a documentation phase)
</success_criteria>

<output>
Create `.planning/phases/10-agents-md/10-01-SUMMARY.md` when done
</output>