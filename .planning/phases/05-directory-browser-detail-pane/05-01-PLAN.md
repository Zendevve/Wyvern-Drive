---
phase: 05-directory-browser-detail-pane
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/DrivePage.tsx
  - web/src/components/FileCard.tsx
  - web/src/components/FileList.tsx
  - web/src/components/DetailPanel.tsx
  - web/src/components/Modal.tsx
  - web/src/components/icons.tsx
  - web/src/hooks/useFolder.ts
  - web/src/api/fs.ts
  - web/src/styles/components.css
  - web/src/styles/tokens.css
autonomous: false
requirements:
  - BROWSE-01
  - BROWSE-02
  - BROWSE-03
  - BROWSE-04
  - DETAIL-01
  - DETAIL-02
  - DETAIL-03
must_haves:
  truths:
    - "Filter chips render horizontally with category icons, names, and counts"
    - "Clicking a filter chip filters the file grid/list to that category"
    - "Folder cards show favorite star toggle and avatar pile mockup"
    - "Create folder card appears first in grid with dotted outline and + icon"
    - "Detail pane has collapse/expand toggle in topbar with smooth animation"
    - "Detail pane shows visual preview (image/video/audio/document) for selected file"
    - "All new UI uses Artano monochrome tokens and Outfit typography"
  artifacts:
    - path: "web/src/components/DrivePage.tsx"
      provides: "Filter chips, create folder card, collapsible detail pane integration"
    - path: "web/src/components/FileCard.tsx"
      provides: "Enhanced folder card with favorite star and avatar pile"
    - path: "web/src/components/FileList.tsx"
      provides: "Filter support, create folder row"
    - path: "web/src/components/DetailPanel.tsx"
      provides: "Visual preview, collapsible logic, enhanced metadata"
    - path: "web/src/styles/components.css"
      provides: "Filter chip styles, avatar pile, create folder card, preview styles"
    - path: "web/src/hooks/useFolder.ts"
      provides: "Category filter parameter support"
    - path: "web/src/api/fs.ts"
      provides: "Filtered folder listing endpoint"
  key_links:
    - from: "web/src/components/DrivePage.tsx"
      to: "web/src/components/FileCard.tsx"
      via: "ContextCard wrapper renders FileCard with context menu"
      pattern: "ContextCard.*FileCard"
    - from: "web/src/components/DrivePage.tsx"
      to: "web/src/components/DetailPanel.tsx"
      via: "Renders DetailPanel when node selected"
      pattern: "DetailPanel"
    - from: "web/src/hooks/useFolder.ts"
      to: "web/src/api/fs.ts"
      via: "Calls getFolder with filter parameter"
      pattern: "getFolder.*filter"

---

<objective>
Enhance the directory browser with category filter chips, premium folder cards (favorite star, avatar piles), create folder card, and upgrade the detail pane with collapsible behavior and visual previews for images, videos, audio, and documents.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
@~/.gemini/antigravity/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-directory-browser-detail-pane/05-CONTEXT.md
@web/src/components/DrivePage.tsx
@web/src/components/FileCard.tsx
@web/src/components/FileList.tsx
@web/src/components/DetailPanel.tsx
@web/src/components/Modal.tsx
@web/src/components/icons.tsx
@web/src/hooks/useFolder.ts
@web/src/api/fs.ts
@web/src/styles/components.css
@web/src/styles/tokens.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add category filter chips to DrivePage</name>
  <files>web/src/components/DrivePage.tsx, web/src/styles/components.css</files>
  <read_first>web/src/components/DrivePage.tsx, web/src/styles/components.css</read_first>
  <action>
    - Add filter chip state to DrivePage: `activeFilter` (string: 'all' | 'documents' | 'images' | 'videos' | 'audio' | 'others')
    - Fetch category counts from useStorageStats hook (already available via sidebar)
    - Render horizontal filter chip row below breadcrumb, above file grid/list
    - Each chip: icon + name + count badge, active/inactive states
    - On click: set activeFilter, trigger refetch with filter
    - Add staggered entrance animation (50ms delay per chip)
    - Keyboard accessible: arrow keys to navigate, Enter to select
  </action>
  <verify>npm run build in web/ directory passes</verify>
  <acceptance_criteria>
    - Filter chips render with correct icons, names, counts
    - Active chip has white background, black text
    - Clicking chip filters file list (visual verification)
    - Chips animate in with stagger
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Enhance FileCard with favorite star and avatar pile</name>
  <files>web/src/components/FileCard.tsx, web/src/styles/components.css</files>
  <read_first>web/src/components/FileCard.tsx, web/src/styles/components.css</read_first>
  <action>
    - Add `isFavorite` and `onToggleFavorite` props to FileCard
    - Add favorite star button (top-right of card): outline star → filled white on hover/active
    - Add avatar pile mockup (bottom-left): 3 overlapping circles with initials, last shows "+N"
    - Use CSS for avatar pile: absolute positioning, negative margins, z-index stacking
    - Star toggle persists to backend (new API call to update node)
    - Maintain existing hover/selection states
    - Add micro-animation on star toggle (scale 0.9 → 1.1)
  </action>
  <verify>npm run build in web/ directory passes</verify>
  <acceptance_criteria>
    - Favorite star renders and toggles visually
    - Avatar pile shows 3 overlapping circles with +N indicator
    - Star toggle calls API (verify in Network tab)
    - Existing hover/selection still work
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add create folder card to grid and list views</name>
  <files>web/src/components/DrivePage.tsx, web/src/components/FileList.tsx, web/src/styles/components.css</files>
  <read_first>web/src/components/DrivePage.tsx, web/src/components/FileList.tsx</read_first>
  <action>
    - Create CreateFolderCard component (inline in DrivePage or separate)
    - Grid: Dotted border card (2px dashed var(--border-subtle)), centered + icon, "New Folder" label
    - List: First row with folder icon, "New Folder" name, clickable
    - Click opens folder creation modal (reuse existing Modal component)
    - Modal: input for folder name, create/cancel buttons
    - On create: call API, refetch, close modal
    - Keyboard accessible: focusable, Enter/Space activates
  </action>
  <verify>npm run build in web/ directory passes</verify>
  <acceptance_criteria>
    - Create folder card appears first in grid with dotted outline
    - Click opens modal with folder name input
    - Creating folder adds it to the list
    - Also works in list view
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Enhance DetailPanel with visual preview and collapsible behavior</name>
  <files>web/src/components/DetailPanel.tsx, web/src/components/DrivePage.tsx, web/src/styles/components.css</files>
  <read_first>web/src/components/DetailPanel.tsx, web/src/components/DrivePage.tsx</read_first>
  <action>
    - Add visual preview section to DetailPanel based on mime_type:
      - Images: `<img>` with src from CDN URL, click for lightbox
      - Videos: `<video controls>` with CDN URL source
      - Audio: `<audio controls>` with CSS waveform animation
      - PDF: `<iframe>` or placeholder with download link
      - Others: Large file-type icon with metadata
    - Add collapse/expand toggle button to DrivePage topbar (chevron left/right icon)
    - DetailPanel width transition: 320px ↔ 0 with 200ms ease-out
    - Persist collapse state in localStorage
    - DrivePage: conditionally render DetailPanel, adjust `.drive-page-wrap.has-detail` padding
    - Add CDN status indicator: green dot (valid), yellow (expired), red (error)
    - Enhance metadata display with copy-to-clipboard for CDN URL
  </action>
  <verify>npm run build in web/ directory passes</verify>
  <acceptance_criteria>
    - Detail pane shows image thumbnail, video player, audio player for respective types
    - Collapse toggle in topbar animates pane in/out smoothly
    - State persists across refreshes (localStorage)
    - CDN status indicator shows correct color
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Backend API updates for category filtering and favorite toggle</name>
  <files>web/src/api/fs.ts, web/src/hooks/useFolder.ts</files>
  <read_first>web/src/api/fs.ts, web/src/hooks/useFolder.ts</read_first>
  <action>
    - Add `filter` parameter to `getFolder` API call (category: 'all' | 'documents' | 'images' | 'videos' | 'audio' | 'others')
    - Update useFolder hook to accept filter parameter and pass to API
    - Add `toggleFavorite` API call (PATCH /nodes/:id/favorite)
    - Update useFolder or create new hook for favorite toggle
    - Ensure filter resets to 'all' on folder navigation
  </action>
  <verify>npm run build in web/ directory passes</verify>
  <acceptance_criteria>
    - getFolder accepts filter parameter
    - useFolder passes filter to API
    - toggleFavorite API call works
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 6: Database migration for is_favorite column</name>
  <files>web/src/lib/storage.ts, web/src/lib/crypto.ts</files>
  <read_first>web/src/lib/storage.ts</read_first>
  <action>
    - Add `is_favorite` column to nodes table (BOOLEAN DEFAULT 0)
    - Handle migration in storage initialization
    - Update node type definition if needed
  </action>
  <verify>npm run build in web/ directory passes</verify>
  <acceptance_criteria>
    - Database schema includes is_favorite column
    - Migration runs without errors
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 7: Start local development server</name>
  <action>Run `npm run dev` in the `web` workspace directory in the background.</action>
  <verify>Verification of server startup output</verify>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Enhanced Directory Browser & Detail Pane</what-built>
  <how-to-verify>Visit the local web app dev URL (usually http://localhost:5173). Verify: 1) Filter chips appear and filter files by category, 2) Folder cards show favorite star and avatar pile, 3) Create folder card works in grid and list, 4) Detail pane has visual previews for images/video/audio, 5) Detail pane collapses/expands with smooth animation, 6) All styling matches Artano monochrome theme.</how-to-verify>
  <resume-signal>Type "approved" to continue</resume-signal>
</task>

</tasks>

<verification>
- [ ] npm run build in web/ succeeds without TypeScript or compilation errors.
- [ ] Visual verification of filter chips, folder cards, create folder, detail pane previews, and collapse animation is approved.
</verification>

<success_criteria>
- All tasks completed successfully.
- Web app builds without errors.
- Directory browser has category filter chips, premium folder cards, create folder card.
- Detail pane is collapsible with visual previews for media files.
- All new UI uses Artano design system tokens.
</success_criteria>

<output>
After completion, create `.planning/phases/05-directory-browser-detail-pane/05-01-SUMMARY.md`
</output>