---
phase: 10-reliability-recovery-local-first-crash-surviving
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - web/src/hooks/useUploader.ts
  - web/src/hooks/useResumableUploader.ts
  - web/src/components/DiagnosticsDrawer.tsx
  - web/src/App.tsx
  - web/src/lib/__tests__/operationQueue.test.ts
autonomous: true
requirements:
  - REL-01
  - REL-02
  - REL-03
  - REL-05
  - OPS-01
must_haves:
  truths:
    - "Pressing Ctrl+Shift+D toggles the diagnostic panel drawer overlay"
    - "Diagnostic panel drawer displays the queue list with status and manual retry/pause controls"
    - "Clicking 'Simulate Crash' throws mid-operation; on refresh, the queue resumes"
    - "Offline mutations (folder creation, renaming) update VFS locally first, then sync online"
  artifacts:
    - path: "web/src/components/DiagnosticsDrawer.tsx"
      provides: "Diagnostics overlay panel with queue controls and logs"
      min_lines: 100
    - path: "web/src/lib/__tests__/operationQueue.test.ts"
      provides: "Unit tests for queue storage, worker idempotency, and offline state"
      min_lines: 100
---

<objective>
Integrate mutating API calls with the OperationQueue, implement the Ctrl+Shift+D diagnostics drawer, and add crash recovery tests.

Purpose: Complete the local-first loop by routing mutations through the durable queue, exposing queue operations to a hidden drawer for developer visibility and recovery actions, and validating reliability invariants with automated tests.
Output: Integrated hook files, UI Drawer component, app entry integration, and test file.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-reliability-recovery-local-first-crash-surviving/10-CONTEXT.md
@.planning/phases/10-reliability-recovery-local-first-crash-surviving/10-01-PLAN.md
@web/src/hooks/useUploader.ts
@web/src/hooks/useResumableUploader.ts
@web/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hook mutating API calls to the OperationQueue</name>
  <files>web/src/hooks/useUploader.ts, web/src/hooks/useResumableUploader.ts</files>
  <read_first>web/src/hooks/useUploader.ts, web/src/hooks/useResumableUploader.ts, web/src/lib/operationQueue.ts</read_first>
  <action>
    Modify mutating UI actions to delegate to `OperationQueue.enqueue()` rather than making direct HTTP requests:
      - Folder creation, node deletion, and renaming must be enqueued.
      - During offline states, update React Query VFS cache immediately with optimistic updates so the UI reflects folder creations and renames instantly.
      - Hook the chunk upload pipeline:
        - When an upload starts, enqueue a parent 'upload' task.
        - Chunk uploads must use the `useResumableUploader` (TUS offset resume).
        - If a crash happens, on restart the worker resumes the upload using the existing TUS session ID and offset, querying the server for the correct next byte.
  </action>
  <verify>tsc --noEmit passes</verify>
  <done>Mutations and resumable chunk uploads successfully route through the OperationQueue</done>
</task>

<task type="auto">
  <name>Task 2: Build the Diagnostics Drawer UI</name>
  <files>web/src/components/DiagnosticsDrawer.tsx, web/src/App.tsx</files>
  <read_first>web/src/App.tsx, web/src/store/queue.ts</read_first>
  <action>
    Create `<DiagnosticsDrawer />` component:
      - Sliding drawer overlay from the right, styled matching dark mode/glassmorphism theme.
      - Keyboard shortcut listener in `App.tsx`: intercept `Ctrl+Shift+D` to toggle the drawer.
      - Content of the drawer:
        - Connection status display (Online/Offline, latencies).
        - Queue state controls (Pause/Resume buttons).
        - List of operations currently in the queue (pending, in_flight, succeeded, failed, cancelled) with details (type, size, retry count, error).
        - Individual operation action buttons: "Retry" (for failed), "Delete" (remove from queue).
        - A "Simulate Crash" button: sets a flag in localStorage that forces the next file chunk upload or folder creation API call to throw an error/abort immediately (simulating page reload/crashed browser), allowing the user to test resume logic.
  </action>
  <verify>tsc --noEmit passes</verify>
  <done>Diagnostics drawer is togglable, functional, and maps queue actions correctly</done>
</task>

<task type="auto">
  <name>Task 3: Implement Unit and Integration Tests for Reliability</name>
  <files>web/src/lib/__tests__/operationQueue.test.ts</files>
  <read_first>web/src/lib/operationQueue.ts</read_first>
  <action>
    Write Vitest tests to cover:
      - Queue DB insertion and retrieval.
      - FIFO order worker execution.
      - Idempotency key deduplication (enqueuing the same upload twice only uploads once).
      - Offline queuing and automatic synchronization when transitioning online.
      - Crash/resume recovery (mocking a crash mid-upload and verifying it resumes from the last chunk).
  </action>
  <verify>npm run test web/src/lib/__tests__/operationQueue.test.ts</verify>
  <done>All reliability unit tests pass successfully</done>
</task>

</tasks>

<verification>
- [ ] npm run build succeeds
- [ ] npm run test passes for the operationQueue tests
</verification>

<success_criteria>
- Build succeeds without errors
- All queue and upload tests pass
- Ctrl+Shift+D displays the diagnostics panel with all controls functional
</success_criteria>

<output>
After completion, create `.planning/phases/10-reliability-recovery-local-first-crash-surviving/10-02-SUMMARY.md`
</output>
