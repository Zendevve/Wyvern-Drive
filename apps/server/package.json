{
  "name": "@disbox/server",
  "version": "0.0.1",
  "private": true,
  "description": "Disbox v2 server - Hono + discord.js-selfbot-v13 + Drizzle + better-sqlite3. Filled in Phase 2.",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "dev": "echo 'server: not yet scaffolded (Phase 2)' && exit 0",
    "build": "echo 'server: stub build (Phase 2 fills this)' && exit 0",
    "start": "echo 'server: stub start' && exit 0",
    "lint": "echo 'server: stub lint (Phase 2)' && exit 0",
    "typecheck": "echo 'server: stub typecheck (Phase 2)' && exit 0",
    "test": "echo 'server: stub test (Phase 2)' && exit 0",
    "clean": "rm -rf dist .turbo data/*.db"
  },
  "dependencies": {
    "@disbox/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.4.5"
  }
}
