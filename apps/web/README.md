# @disbox/web

Next.js 14 file manager for Disbox v2. Filled in during **Phase 3**.

Stack: Next.js 14 (App Router) + TypeScript + Tailwind + shadcn/ui + Zustand.

The web app talks to `@disbox/server` over REST, uses `@disbox/shared` for chunking/hashing/tree-codec.
