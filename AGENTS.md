# Agent skills

### Issue tracker

Issues live as GitHub issues in Zendevve/Wyvern-Drive via `gh`. See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical roles mapped 1:1 to tracker labels. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout (`CONTEXT.md` + `docs/adr/`). See `docs/agents/domain.md`.

### Session learnings

After every session, append takeaways to `docs/agents/learnings.md`: what worked, what to improve, bottlenecks to avoid. See that file for format. Read it at session start when touching related areas.
